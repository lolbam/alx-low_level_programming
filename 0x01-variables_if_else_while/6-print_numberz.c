#include <stdio.h>
/**
 * main - prints the alphabet in lowercase, followed by a new line.
 * Return: returns 0 always
 */

int main(void)
{
	int p;

	for (p = ; p <= 9; p++)
	{
		putchar(p);
	}
	putchar('\n');
	return (0);
}
