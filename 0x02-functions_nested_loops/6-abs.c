#include "main.h"

/**
 * _abs - computes the absolute value of the integer
 *@n: is the integer
 * Return: Returns 0
 */
int _abs(int n)
{ 

	if (n >= 0)
	{
		return(n);
	}
	else
	{
		return(-n);
	}
}
